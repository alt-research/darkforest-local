name: Docker build and push to azure storage

on:
  push:
    branches:
      - 'master'

jobs:
  production-static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS_PRD }}

      - name: Build and push to storage account
        uses: azure/CLI@v1
        with:
          inlineScript: |
            ./scripts/ci_build_deploy.sh && az storage blob upload-batch --overwrite --account-name darkforestprod --auth-mode key -d '$web' -s "client/dist"
      
      - name: logout
        run: |
          az logout
        if: always()

